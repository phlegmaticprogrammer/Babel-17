def odd x = x mod 2 == 1
def even x = x mod 2 == 0

match [1,2,3,4]
case [odd?, even?, odd?, even?] => #log Yes
case _ => #log No
end


def complex (x, y) = {x = x, y = y, deconstruct_ = Energy => x*x+y*y}


match complex (3, 5)
case (Energy ? e) => #log Yes e
case r => #log No r
end

